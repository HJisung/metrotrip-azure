# 실제 배포 도메인의 HTTPS 요청을 Traefik을 통해 프론트엔드로 전달한다.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: metrotrip
  namespace: metrotrip
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - metrotrip.example.com
      secretName: metrotrip-tls
  rules:
    - host: metrotrip.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 5173
